# Fixtures

Harness recordings (`*.jsonl`) replayed by `anna-app dev` and verified with
`anna-app fixture verify <file>`.

Referenced by `manifest.json` -> `dev.fixtures: ["fixtures/*.jsonl"]`.

Populated in **Gelombang 3** (task H-partial: JSONL fixtures for the read
tools). Empty for CP-0 — the ping/pong harness needs no recordings.
